<footer>
    <nav class="pagination" role="navigation">
        {{#if prev}}
        <a class="page previous newer-posts" href="{{page_url prev}}">Neue Beiträge</a>
        {{/if}}
        <span class="page-number">Seite {{page}} von {{pages}}</span>
        {{#if next}}
        <a class="page next older-posts" href="{{page_url next}}">Ältere Beiträge</a>
        {{/if}}
    </nav>
</footer>
